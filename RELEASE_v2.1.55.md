# Release v2.1.55 - 用户现场部署版本校正说明

> **口径校正日期**: 2026-07-01  
> **正式标签**: `v2.1.55 -> 7556710`  
> **历史云端基线标签**: `v2.1.55-cloud-base -> 93e6640`  
> **说明**: 本文件用于回溯校正 `v2.1.55` 的 Git 口径，不代表 2026-07-01 新增了一轮业务功能发布。

---

## 这次到底校正了什么

- `v2.1.55` 现在明确代表**用户现场实际部署版本**。
- `v2.1.55-cloud-base` 单独保留，代表**历史上云端原始的 `release: v2.1.55` 提交**。
- 仓库分支策略同步固定为：
  - `main` = 用户正式 / 部署版本
  - `dev` = 后续开发版本

---

## 为什么要这样校正

复盘 `assembly-manual_images_v2.1.55.tar` 后确认：

1. **后端**与 `93e6640` 完全一致。
2. **前端**已经带有 `自动翻页 / 停止翻页`，而 `93e6640` 的前端仍是 `自动播放 / 停止自动播放`。

也就是说，用户现场部署的 `v2.1.55` 不是简单等于云端当年的 `93e6640`，而是更接近后来恢复出来的部署版提交 `7556710`。

---

## 现在怎么理解这两个标签

### `v2.1.55`

- 指向提交 `7556710`
- 含义：用户现场当前部署版本
- 适用场景：需要回到客户正在用的正式代码时，优先 checkout 这个 tag

### `v2.1.55-cloud-base`

- 指向提交 `93e6640`
- 含义：历史上云端原始 `release: v2.1.55` 基线
- 适用场景：需要追查“云端最初写成 `release: v2.1.55` 的那份代码”时使用

---

## 后续 Git 工作流

1. 日常开发在 `dev`
2. 需要上线时，将 `dev` 合并到 `main`
3. `main` 始终尽量保持“用户当前正式/部署版本”口径
4. 正式版本再按实际发布结果打 tag

---

## 一句话结论

从 2026-07-01 起，仓库里凡是提到 `v2.1.55`，默认都应理解为**用户现场实际部署版本 (`7556710`)**；如果要讨论历史云端原始提交，请明确使用 `v2.1.55-cloud-base`。
