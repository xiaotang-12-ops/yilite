# Release v2.0.77 (2026-01-10)

## 亮点
- 任务全链路恢复与兜底：生成任务状态落盘，刷新可恢复；查看器支持展示失败/处理中任务；生成页、查看器均提供“中断/删除”入口便于清理卡住任务。
- 生成流程自清理：同名任务但缺少已发布手册时自动清理残留目录后再启动，避免“任务已存在”挡住重跑。
- 问题记录：补充 `.cursor/step_hang_findings.md`，说明超大 STEP 卡死的原因与防护建议。

## 变更详情（自 v2.0.74 起）
- v2.0.77：生成页新增“强制中断/清理”按钮（断开 SSE、调用删除接口并重置状态）；查看器对 `processing` 任务增加“中断/删除”按钮；卡死案例分析文档落地。
- v2.0.76：任务状态写入 `task_status.json` 并可在 `/api/status/{task}` 恢复；`/api/manuals` 支持 `include_failed` 返回未完成/失败任务；查看器可删除失败任务；生成页刷新可自动恢复未完成任务。
- v2.0.75：`/api/generate` 遇到残留目录但无手册时自动清空目录并清理内存记录后重试；若目录已有手册则仍拒绝覆盖。

## 升级与注意事项
- 建议升级后重启后端服务以启用任务状态持久化。
- 对于已卡死的任务，可在生成页或查看器通过“中断/删除”按钮清理后重传。
