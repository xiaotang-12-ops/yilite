# 🎉 Release v2.1.18 - AI智能装配指导平台

> **发布日期**: 2026-02-05  
> **版本范围**: v2.1.0 - v2.1.18  
> **重大更新**: 断点续跑、历史版本管理、调用点独立配置、BOM文本层优先纠正

---

## 🎯 这个版本做了什么？（说人话版）

### 💡 新增功能（让你的工作更轻松）

1. **生成任务可以断点续跑了** 🔄
   - 生成到一半断电/关机？没关系，重启后可以继续，不用从头开始
   - 失败的任务会告诉你原因（余额不足/权限不够/Key错误），不再一脸懵逼
   - 上传页有"继续上一次任务"按钮，点一下就能接着跑

2. **每个AI调用点可以用不同的Key了** 🔑
   - 比如你用NewAPI中转，不同模型需要不同渠道的Key，现在可以分别配置
   - 测试模型时会用正确的Key，不会再出现"测试成功但实际运行失败"的尴尬

3. **BOM识别更准确了** 📄
   - 优先从PDF文本层提取BOM信息（比AI视觉识别准确）
   - 自动纠正AI识别错误（比如把 `01.03.5275` 误识别成 `01.03.5276`）
   - 生成纠正日志，方便你追溯

4. **上传时会检查2D和3D图纸是否匹配** 📊
   - 上传错了图纸？立刻告诉你"2D与3D图纸不匹配"
   - 不用等生成完才发现问题，节省时间

5. **手机端体验更好了** 📱
   - 打开图纸抽屉后点返回键，只关闭抽屉，不会刷新整个页面
   - 爆炸视图不会越点越大了（之前来回切换会越来越夸张）
   - 历史版本预览时不能删除零件了（只读模式，避免误操作）

### 🐛 修复的Bug（之前让你头疼的问题）

1. **调用点独立Key终于生效了** ✅
   - 之前：你配置了独立Key，测试成功，但实际运行还是用全局Key（导致403错误）
   - 现在：配置的独立Key真的会被使用了

2. **任务恢复前会检查文件是否损坏** ✅
   - 之前：镜像重启后，点"继续生成"才发现文件损坏，白等一场
   - 现在：恢复前就检查，损坏了直接提示"请删除后重试"

3. **手机端打开图纸抽屉后点返回不会刷新页面了** ✅
   - 之前：打开图纸抽屉，点返回键，整个页面刷新并跳到第一步
   - 现在：只关闭抽屉，页面不刷新，停留在当前步骤

4. **爆炸视图不会越来越大了** ✅
   - 之前：来回点"默认/分散"，零件飞得越来越远（爆炸倍数从5涨到26）
   - 现在：爆炸倍数固定，不会累积放大

### 🔧 其他优化（细节改进）

- 管理员登录入口移到顶部导航，生成器只有管理员能看到
- Docker镜像体积减少约1.6GB（构建更快）
- 设置页可以快捷选择常用模型（不用手动输入长长的模型ID）
- 失败任务会明确告诉你原因，不再显示"未知错误"
- 手机端隐藏管理员登录按钮，避免误触

---

## 📋 目录

- [✨ 核心新增功能](#-核心新增功能)
- [🐛 重要Bug修复](#-重要bug修复)
- [🔧 优化改进](#-优化改进)
- [📦 完整版本列表](#-完整版本列表)

---

## ✨ 核心新增功能

### 🔄 断点续跑与任务管理 (v2.1.00 - v2.1.03)

**任务断点续跑**
- ✅ 新增 `/api/task/{task_id}/resume` 和 `/api/task/{task_id}/cancel` 接口
- ✅ 任务状态持久化到 `task_status.json`，支持镜像重启后恢复
- ✅ 流水线支持基于 step1-7 JSON 复用继续，避免重复计算
- ✅ 失败原因分类（余额不足/无权限/AI-Key缺失）写入 `failure_hint`

**生成器交互优化**
- ✅ 上传页新增"继续上一次任务"按钮，停止/失败后可直接恢复
- ✅ 进度条实时展示 `progress_message`，轮询 `/api/status` 获取进度
- ✅ 冲突弹窗支持"继续上一次任务/删除失败任务"选择
- ✅ 继续失败时自动提示删除损坏任务，避免死循环

**任务完整性检查** (v2.1.15)
- ✅ 恢复前检查关键中间文件（`step1_bom.json`、`step2_bom_matched.json`、`step4_assembly_steps.json`）
- ✅ 中间文件损坏时返回 `resume_corrupt` 错误，提示用户删除后重试
- ✅ 避免用户点击"继续生成"后才发现任务损坏，浪费时间

---

### 🔑 调用点独立API Key配置 (v2.1.12 - v2.1.14)

**多Key支持**
- ✅ 每个调用点可配置独立API Key（如NewAPI不同渠道Key）
- ✅ 留空则使用对应提供方的默认Key
- ✅ 测试模型时使用正确的Key，避免403错误

**Bug修复**
- ✅ v2.1.13: 修复 `_get_api_key` 方法忽略 `custom_key` 的严重BUG
- ✅ v2.1.14: 彻底修复 `_resolve_call_point` 丢弃 `custom_key` 字段的问题
- ✅ AI匹配失败时立即终止流程，避免浪费费用

---

### 📄 BOM文本层优先纠正机制 (v2.1.11)

**文本层优先策略**
- ✅ PDF文本层提取BOM代号（准确率高于Vision API）
- ✅ 文本层值优先覆盖Vision错误值（如 `01.03.5275` 被误识别为 `01.03.5276`）
- ✅ 生成 `step2_bom_correction_log.json` 记录所有纠正操作
- ✅ 解决BOM代号重复和3D高亮错误问题

**实际效果**
- ✅ 文本层能提取约64%的BOM项（37/58），准确性高于Vision
- ✅ 成功纠正Vision的识别错误，避免后续流程失败

---

### 📊 2D/3D图纸匹配验证 (v2.1.16)

**上传时验证**
- ✅ 检查PDF和STEP文件数量是否一致
- ✅ 提取核心名称并验证每个PDF都有对应的STEP文件
- ✅ 不匹配时返回400错误："当前2D与3D图纸不匹配，请更换后再试"

**匹配规则示例**
```
PDF:  06.84.01.0001T-HSB1220(48IN)-NL-Z吹雪机无连接器.pdf
      → 核心名称: HSB1220(48IN)-NL-Z吹雪机无连接器

STEP: T-HSB1220(48IN)-NL-Z吹雪机无连接器-203.STEP
      → 核心名称: HSB1220(48IN)-NL-Z吹雪机无连接器

✅ 匹配成功
```

---

### 🎨 移动端交互优化 (v2.1.17 - v2.1.18)

**图纸抽屉返回问题彻底修复** (v2.1.17)
- ✅ 修复 `popstate` 监听器直接赋值导致历史回退逻辑未执行的致命漏洞
- ✅ 修复 watch 监听器在 popstate 触发时重复处理的问题
- ✅ 手机端打开图纸抽屉后点击返回键，只关闭抽屉，不刷新页面，不跳转步骤

**爆炸视图累积放大问题修复** (v2.1.17)
- ✅ 缓存原始包围盒的 `maxDim`，避免实时计算导致正反馈循环
- ✅ 添加动画取消机制，避免多个动画同时运行导致位置混乱
- ✅ 添加防抖处理，优化滑块拖动体验

**历史版本预览禁止删除零件** (v2.1.18)
- ✅ 历史版本模式下隐藏"删除零件"按钮
- ✅ `deletePart` 函数添加只读模式检查（防御性编程）
- ✅ 提示"历史版本为只读，无法删除零件"

---

## 🐛 重要Bug修复

### 🔥 调用点Key配置修复 (v2.1.13 - v2.1.14)

**问题描述**
- ❌ 用户为"AI匹配"配置独立Key（有Gemini权限）
- ❌ 测试成功，但实际运行失败（403错误）
- ❌ 原因：Pipeline只使用全局Key，忽略独立Key

**修复方案**
- ✅ v2.1.13: 修改 `_get_api_key` 方法接收完整 `call_point` 对象
- ✅ v2.1.14: 修复 `_resolve_call_point` 返回字典时保留 `custom_key` 字段
- ✅ 增强日志，打印Key来源（独立Key/全局Key）和脱敏后的Key值

---

### 🔄 任务恢复完整性检查 (v2.1.15)

**问题描述**
- ❌ 镜像重启后，中间JSON文件可能损坏（截断、格式错误）
- ❌ 系统只检查最终的 `assembly_manual.json`，不检查中间文件
- ❌ 用户点击"继续生成"后才发现任务损坏，浪费时间

**修复方案**
- ✅ 修改 `_validate_manual_json()` 函数，增加关键中间文件检查
- ✅ 检查3个关键文件：`step1_bom.json`、`step2_bom_matched.json`、`step4_assembly_steps.json`
- ✅ 中间文件损坏时返回 `{"valid": False, "error": "resume_corrupt"}`
- ✅ 前端提示用户："旧任务数据损坏，请删除后重试"

---

### 📱 手机端图纸抽屉返回刷新问题 (v2.1.17)

**问题描述**
- ❌ 手机端查看图纸到第二步以上，打开图纸抽屉，点击返回
- ❌ 整个页面会刷新并跳到第一步

**根本原因**
- ❌ **第一层问题**: 抽屉打开时没有添加历史记录（`pushState`）
- ❌ **第二层问题**: `popstate` 监听器直接赋值关闭抽屉，没有调用 `closeMobileDrawers()`
- ❌ **第三层问题**: watch 监听器没有检查 `closingOverlayFromPopstate` 标记

**修复方案**
- ✅ 修复 `popstate` 监听器：调用 `closeMobileDrawers(true)` 而不是直接赋值
- ✅ 修复 watch 监听器：添加 `closingOverlayFromPopstate` 检查
- ✅ 确保关闭逻辑统一，避免重复处理

---

### 🎨 爆炸视图累积放大问题 (v2.1.17)

**问题描述**
- ❌ 查看器图纸的爆炸视图，放大到一定程度后
- ❌ 点击"默认"/"分散"来回切换，爆炸视图会越来越大
- ❌ `explodeBase` 从 5.657 涨到 26.839（涨了4.7倍）

**根本原因**
- ❌ `updateStepDisplay()` 每次实时计算包围盒（`Box3().setFromObject(model)`）
- ❌ 包围盒包含当前爆炸状态下的零件位置，导致 `maxDim` 越来越大
- ❌ 形成正反馈循环（爆炸 → 包围盒变大 → 下次爆炸更大）

**修复方案**
- ✅ 添加全局变量 `cachedModelMaxDim` 缓存原始包围盒的 `maxDim`
- ✅ 在 `loadModel()` 和 `switchGLBModel()` 中，模型居中后立即计算并缓存
- ✅ 修改 `updateStepDisplay()` 使用缓存的 `cachedModelMaxDim` 替代实时计算
- ✅ 添加动画取消机制和防抖处理

---

## 🔧 优化改进

### 🎯 管理员入口与生成器门禁 (v2.1.06)

- ✅ 顶部导航将"帮助"替换为管理员登录入口
- ✅ 生成器入口仅管理员可见（首页/查看器内按钮同步隐藏）
- ✅ 路由守卫拦截未登录访问 `/generator`
- ✅ 管理员状态统一 Pinia + `sessionStorage`

---

### 🚀 镜像体积减负 (v2.1.07 - v2.1.08)

**构建上下文优化** (v2.1.07)
- ✅ `.dockerignore` 增加 `output_archive/`，防止历史归档产物进入镜像

**白名单COPY** (v2.1.08)
- ✅ 后端 `Dockerfile` 改为白名单复制核心源码目录
- ✅ 镜像体积下降约 1.6GB

---

### 🎨 模型快捷选择 (v2.1.09)

- ✅ 设置页"调用点模型配置"新增"快捷模型"下拉
- ✅ 选择 `gemini3-flash` 自动填充 `google/gemini-3-flash-preview`
- ✅ 仅在提供方为豆包（NewAPI）时展示，避免误选

---

### 🛡️ 失败即失败 + 断点校验强化 (v2.1.10)

- ✅ Pipeline 增加步骤有效性校验：Step1~Step7 结果无效则重跑
- ✅ 手册整合前强制校验步骤数，`steps=0` 直接判失败
- ✅ 后端对 `assembly_manual.json` 做内容校验（空手册视为失败）
- ✅ 前端失败态只允许"余额不足/取消"继续，未知错误提示"文件错误"仅删除

---

### 📱 手机端隐藏管理员登录按钮 (v2.1.17)

- ✅ 手机端隐藏管理员登录按钮，避免移动端误触
- ✅ 桌面端不受影响

---

## 📦 完整版本列表

### v2.1.18 (2026-02-05)
- **历史版本预览禁止删除零件**: 历史版本模式下隐藏删除按钮，添加只读模式检查

### v2.1.17 (2026-02-05)
- **手机端图纸抽屉返回刷新问题彻底修复**: 修复 popstate 监听器和 watch 监听器的致命漏洞
- **爆炸视图累积放大问题修复**: 缓存原始包围盒，避免正反馈循环
- **手机端隐藏管理员登录按钮**: 避免移动端误触

### v2.1.16 (2026-02-03)
- **新增文件上传时的2D/3D图纸匹配验证**: 上传时检查PDF和STEP文件名是否匹配

### v2.1.15 (2026-02-03)
- **增强任务恢复前的完整性检查**: 检查关键中间文件，避免恢复损坏任务

### v2.1.14 (2026-02-03)
- **彻底修复调用点独立Key丢失问题**: 修复 `_resolve_call_point` 丢弃 `custom_key` 字段

### v2.1.13 (2026-02-03)
- **修复调用点独立Key未生效的严重BUG**: 修改 `_get_api_key` 方法接收完整 `call_point` 对象
- **AI匹配失败时立即终止流程**: 避免浪费费用

### v2.1.12 (2026-02-02)
- **调用点独立API Key配置**: 每个调用点可配置独立Key（如NewAPI不同渠道Key）

### v2.1.11 (2026-02-02)
- **Step2 BOM文本层优先纠正机制**: 文本层值优先覆盖Vision错误值

### v2.1.10 (2026-01-30)
- **失败即失败 + 断点校验强化**: Pipeline增加步骤有效性校验，空手册视为失败

### v2.1.09 (2026-01-29)
- **模型快捷选择**: 设置页新增"快捷模型"下拉，自动填充模型ID

### v2.1.08 (2026-01-29)
- **镜像体积减负（白名单COPY）**: 后端Dockerfile改为白名单复制，镜像体积下降约1.6GB

### v2.1.07 (2026-01-29)
- **镜像体积减负（构建上下文）**: `.dockerignore` 增加 `output_archive/`

### v2.1.06 (2026-01-29)
- **管理员入口与生成器门禁**: 顶部导航新增管理员登录入口，生成器仅管理员可见

### v2.1.05 (2026-01-29)
- **后端立即失败修复**: 修复 `IndentationError` 和 `UnboundLocalError`

### v2.1.04 (2026-01-29)
- **上次任务信息提示补齐**: 上传页展示任务名称、状态、更新时间

### v2.1.03 (2026-01-28)
- **失败任务手动删除入口**: 上传页新增"删除上一次任务"按钮

### v2.1.02 (2026-01-28)
- **继续失败自动删除提示强化**: 继续任务失败时触发删除确认提示

### v2.1.01 (2026-01-28)
- **生成器继续入口**: 上传页新增"继续上一次任务"按钮
- **恢复日志页 + 覆盖率重试修复**: 修复Agent3/Agent4重试提示崩溃
- **平台名字修改**: AI智能装配平台改为AI智能装配指导

### v2.1.00 (2026-01-28)
- **断点续跑与失败任务识别**: 新增 `/api/task/{task_id}/resume` 和 `/api/task/{task_id}/cancel`
- **流水线断点续跑**: 支持基于 step1-7 JSON 复用继续
- **前端生成器进度条**: 日志黑框下方新增进度条，轮询展示进度

---

## 🔗 相关链接

- **GitHub仓库**: https://github.com/xiaotang-12-ops/yilite.git
- **Tag**: v2.1.18
- **文档**: 查看 `Memory_Development/changelog.md` 获取详细技术细节

---

## 👥 贡献者

感谢所有为本版本做出贡献的开发者！

---

## 📝 升级说明

### 从 v2.0.x 升级到 v2.1.18

1. **数据库迁移**: 无需迁移
2. **配置更新**: 
   - 新增调用点独立Key配置（可选）
   - 检查 `.env` 文件是否包含所需的API Key
3. **Docker镜像**: 重新构建镜像以应用体积优化
4. **任务恢复**: 旧任务可能需要重新生成（中间文件格式变更）

### 注意事项

- ⚠️ 历史版本预览现在为只读模式，无法删除零件
- ⚠️ 生成器入口仅管理员可见，需要先登录
- ⚠️ 调用点独立Key配置为可选功能，不影响现有配置

---

**完整更新日志**: [v2.1.0...v2.1.18](https://github.com/xiaotang-12-ops/yilite/compare/v2.1.0...v2.1.18)
