# Agent 状态流转图

## 状态机定义

```
┌─────────┐
│  idle   │  初始状态
└────┬────┘
     │ 收到开始信号
     │ log_agent_call(agent, action, "running")
     ▼
┌─────────┐
│ running │  工作中
└────┬────┘
     │
     ├─────────────────┬─────────────────┬─────────────────┬─────────────────┐
     │                 │                 │                 │                 │
     ▼                 ▼                 ▼                 ▼                 ▼
┌─────────┐      ┌─────────┐      ┌─────────┐      ┌─────────┐      ┌─────────┐
│ success │      │ warning │      │ skipped │      │  error  │      │ timeout │
└─────────┘      └─────────┘      └─────────┘      └─────────┘      └─────────┘
  绿色              橙色              黄色              红色              灰色
  ✅               ⚠️               ⏭️               ❌               ⏱️
```

## Agent 5 的特殊状态流转

### 场景1：组件有图片（正常流程）

```
组件装配工 #1
  idle → running → success ✅

组件装配工 #2
  idle → running → success ✅

组件装配工 #3
  idle → running → success ✅
```

### 场景2：组件没有图片（跳过流程）⭐ 修复后

```
组件装配工 #1
  idle → running → skipped ⏭️  (新增！)

组件装配工 #2
  idle → running → skipped ⏭️  (新增！)

组件装配工 #3
  idle → running → skipped ⏭️  (新增！)
```

### 场景3：部分组件有图片（混合流程）

```
组件装配工 #1
  idle → running → success ✅

组件装配工 #2
  idle → running → skipped ⏭️

组件装配工 #3
  idle → running → success ✅
```

## 日志信号与状态映射

### 开始信号 → `running`

| 日志内容 | 状态变化 |
|---------|---------|
| `👷 文件管理AI员工加入工作` | idle → running |
| `👷 BOM分析AI员工加入工作` | idle → running |
| `👷 装配规划AI员工加入工作` | idle → running |
| `👷 3D模型AI员工加入工作` | idle → running |
| `👷 组件装配工 #N加入工作` | idle → running |
| `👷 产品总装AI员工加入工作` | idle → running |
| `👷 焊接工程师AI员工加入工作` | idle → running |
| `👷 安全专员AI员工加入工作` | idle → running |
| `👷 手册编辑AI员工加入工作` | idle → running |

### 完成信号 → 终态

| 日志内容 | 状态变化 |
|---------|---------|
| `✅ [Agent]完成了工作` | running → success |
| `⚠️ [Agent]跳过了工作` | running → skipped |
| `❌ [Agent]工作失败` | running → error |

## 前端实现示例

### Vue 3 组件示例

```vue
<template>
  <div class="agent-card" :class="agentStatus">
    <div class="agent-header">
      <span class="agent-icon">{{ agentIcon }}</span>
      <span class="agent-name">{{ agentName }}</span>
      <span class="agent-status-badge">{{ statusText }}</span>
    </div>
    <div class="agent-progress" v-if="agentStatus === 'running'">
      <div class="progress-bar" :style="{ width: progress + '%' }"></div>
    </div>
    <div class="agent-message">{{ currentMessage }}</div>
  </div>
</template>

<script setup>
import { ref, computed } from 'vue'

const props = defineProps({
  agentId: Number,
  agentName: String,
  agentIcon: String
})

const agentStatus = ref('idle')
const currentMessage = ref('')
const progress = ref(0)

const statusText = computed(() => {
  const statusMap = {
    idle: '等待中',
    running: '工作中',
    success: '已完成',
    warning: '完成(有警告)',
    skipped: '已跳过',
    error: '失败'
  }
  return statusMap[agentStatus.value] || '未知'
})

// 监听SSE消息
function handleAgentLog(log) {
  // 开始信号
  if (log.includes('加入工作')) {
    agentStatus.value = 'running'
    currentMessage.value = log
  }
  
  // 完成信号
  if (log.includes('✅') && log.includes('完成了工作')) {
    agentStatus.value = 'success'
    progress.value = 100
  }
  
  // 跳过信号 ⭐ 新增
  if (log.includes('⚠️') && log.includes('跳过了工作')) {
    agentStatus.value = 'skipped'
    progress.value = 100
  }
  
  // 失败信号
  if (log.includes('❌') && log.includes('失败')) {
    agentStatus.value = 'error'
  }
  
  // 进度更新
  const progressMatch = log.match(/(\d+)%/)
  if (progressMatch) {
    progress.value = parseInt(progressMatch[1])
  }
}
</script>

<style scoped>
.agent-card {
  border-left: 4px solid #9CA3AF;
  padding: 16px;
  margin-bottom: 12px;
  background: white;
  border-radius: 8px;
  transition: all 0.3s ease;
}

.agent-card.running {
  border-left-color: #3B82F6;
  animation: pulse 2s infinite;
}

.agent-card.success {
  border-left-color: #10B981;
}

.agent-card.warning {
  border-left-color: #F59E0B;
}

.agent-card.skipped {
  border-left-color: #FCD34D;
  opacity: 0.7;
}

.agent-card.error {
  border-left-color: #EF4444;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.8; }
}

.agent-status-badge {
  padding: 4px 12px;
  border-radius: 12px;
  font-size: 12px;
  font-weight: 500;
}

.agent-card.running .agent-status-badge {
  background: #DBEAFE;
  color: #1E40AF;
}

.agent-card.success .agent-status-badge {
  background: #D1FAE5;
  color: #065F46;
}

.agent-card.skipped .agent-status-badge {
  background: #FEF3C7;
  color: #92400E;
}

.agent-card.error .agent-status-badge {
  background: #FEE2E2;
  color: #991B1B;
}
</style>
```

## 8个Agent卡片的完整时间线

```
时间轴：22:02:41 ─────────────────────────────────────────────► 22:05:19

┌─────────────────────────────────────────────────────────────────────────┐
│ Agent 1: 📂 文件管理员                                                    │
│ ████████ (22:02:41 - 22:02:42) ✅ success                                │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ Agent 2: 📊 BOM数据分析员                                                 │
│         ████████████ (22:02:42 - 22:02:52) ✅ success                    │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ Agent 3: 🔍 装配规划师                                                    │
│                     ████████ (22:02:52 - 22:03:02) ✅ success            │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ Agent 4: 🎨 3D模型工程师                                                  │
│                             ████████████████ (22:03:02 - 22:04:08) ✅    │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ Agent 5: 🔨 组件装配工程师                                                │
│   #1 ⏭️ skipped (22:04:08)                                               │
│   #2 ⏭️ skipped (22:04:08)                                               │
│   #3 ⏭️ skipped (22:04:08)                                               │
│   #4 ⏭️ skipped (22:04:08)                                               │
│   #5 ⏭️ skipped (22:04:08)                                               │
│   #6 ⏭️ skipped (22:04:08)                                               │
│   #7 ⏭️ skipped (22:04:08)                                               │
│                                                 (22:04:08) ⚠️ warning    │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ Agent 6: 🏗️ 产品总装工程师                                               │
│                                                 ████████ (22:04:08 -     │
│                                                 22:04:35) ✅ success      │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ Agent 7a: ⚡ 焊接工程师                                                   │
│                                                         ████████          │
│                                                 (22:04:35 - 22:05:02) ✅ │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ Agent 7b: 🛡️ 安全专员                                                    │
│                                                                 ████████  │
│                                                 (22:05:02 - 22:05:19) ✅ │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ Agent 8: 📚 手册编辑员                                                    │
│                                                                 ████ ✅   │
│                                                         (22:05:19) ✅     │
└─────────────────────────────────────────────────────────────────────────┘
```

## 关键要点

### ✅ 修复前 vs 修复后

**修复前（问题）：**
```
Agent 5: 🔨 组件装配工程师
  #1 running... (永远停留在这里！)
  #2 running... (永远停留在这里！)
  #3 running... (永远停留在这里！)
```

**修复后（正确）：**
```
Agent 5: 🔨 组件装配工程师
  #1 running → skipped ⏭️
  #2 running → skipped ⏭️
  #3 running → skipped ⏭️
  总结: 成功处理 0/7, 跳过 7/7
```

### 🎯 核心原则

1. **每个Agent必须有终态**：success / warning / skipped / error
2. **不能停留在running状态**：必须有明确的完成信号
3. **前端要有超时保护**：如果5分钟没更新，显示警告
4. **状态要清晰可见**：用颜色和图标区分不同状态

