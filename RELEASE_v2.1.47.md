# 🎉 Release v2.1.47 - AI智能装配指导平台

> **发布日期**: 2026-03-13  
> **版本范围**: v2.1.43 - v2.1.47  
> **重大更新**: 内网 HTTPS 扫码、PDF 文本层 BOM 固定 6 列提取、产品总装匹配收口、步骤标题口径统一

---

## 🎯 这个版本做了什么？（说人话版）

### 💡 新增和增强的能力

1. **手机扫码现在可以在内网 HTTPS 场景真正落地了** 📱
   - 前端新增 `3443` HTTPS 入口，支持自签证书部署
   - API、WebSocket、SSE 全部改成同源协议，不再写死 `localhost`
   - Android 系统浏览器在首次信任证书后，可以直接扫码并把物料代码回填到搜索框

2. **PDF 文本层 BOM 现在更稳了** 📄
   - 主链路固定提取 `seq/code/product_code/name/material/quantity` 这 6 列关键字段
   - 不再因为重量列缺失、错位、漂移就把整行 BOM 丢掉
   - 文本层提取到的 `material` 和 `quantity` 会回灌到后续生成流程，纠正 Vision 误读

3. **产品总装的 BOM/3D 匹配终于开始像人一样收口了** 🧠
   - 层级匹配结果锁定为底座，后续匹配只能补点，不能再把已有总成节点冲掉
   - 最后一层补漏改成复用同一个匹配模型做 AI 补漏，不再继续堆规则补丁
   - 标准件异名、短中文锚点、尾部脏数据这几类高频坑都做了收口

4. **一步一个料的归属规则更硬了** 🔩
   - 产品步骤会按 `bom_seq` 强制收口
   - 不属于当前步骤的零件和紧固件会被剔除，避免“未来物料提前出现在前面步骤”
   - 查看器当前步骤只高亮“第一次在本步出现”的节点，不再出现“前面黄、后面蓝、再后面又黄”的冲突

5. **生成出来的步骤标题更干净了** ✏️
   - 标题只允许“动作 + 零件名”
   - 数量、规格、括号、力矩这类信息只能写进 `description`
   - `NewAPI` 快捷模型新增 `qwen3.5-plus-2026-02-15`，设置时少手输一个长模型 ID

---

## 🐛 这次重点修了哪些坑

### 1. BOM 最后一条被表尾脏数据污染

- 之前：最后一条 BOM 可能把 `工艺路线`、`技术要求` 一起吞进去，导致数量错、名称脏
- 现在：新增尾部截断规则，`seq 1` 这类错误数量问题已经按真实图纸回正

### 2. 产品总装步骤串件

- 之前：同一个 `bom_seq` 可能被塞进多个步骤，像 `NAUO71` 会在错误步骤提前高亮
- 现在：后处理按 `step_number + bom_mapping_table` 强制校验归属，不属于当前步骤的条目直接剔除

### 3. 层级匹配优先级不够硬

- 之前：先匹配到的总成节点，后面可能又被散件匹配或 AI 结果覆盖掉
- 现在：层级匹配结果锁定为底座，后续只允许增量补点，不能覆盖

### 4. 只有文字没有 3D 绑定时像“假成功”

- 之前：步骤即使没绑定到 3D 节点，看上去也像生成成功
- 现在：手册步骤会显式输出 `has_3d_binding`、`missing_node_binding_codes`、`binding_warning`

### 5. 标题带数量和规格，不适合工人阅读

- 之前：模型会输出 `安装油杯（8个）` 这类标题，口径不统一
- 现在：组件模式和产品模式提示词统一限制，标题只保留动作和零件名

---

## 🔧 这版具体包含哪些版本

### v2.1.47 (2026-03-11)

- **步骤标题去数量口径 + NewAPI 快捷模型补充**
- 统一组件/产品模式标题规则，禁止标题写数量、规格、括号
- 设置页 `NewAPI` 快捷模型新增 `qwen3.5-plus-2026-02-15`

### v2.1.46 (2026-03-10)

- **产品总装步骤归属锁定 + 重复节点高亮收口**
- 新增步骤级 `bom_seq` 归属收口，跨步骤串件会被清掉
- 查看器只高亮当前步骤首次出现的节点，颜色语义恢复正常

### v2.1.45 (2026-03-10)

- **BOM 匹配收口二次重构**
- 最终兜底从规则改成复用当前模型做 AI 补漏
- PDF 文本层 BOM 升级为固定 6 列提取：`seq/code/product_code/name/material/quantity`

### v2.1.44 (2026-03-10)

- **产品总装匹配收口修复**
- 修复最后一条 BOM 被尾部脏数据污染
- 层级匹配改为底座锁定
- 新增标准件异名兜底与未绑 3D 步骤告警

### v2.1.43 (2026-03-09)

- **Viewer 内网 HTTPS 扫码实装验证**
- 前端切到同源 API/WS/SSE
- Nginx 增加 `443 ssl`
- `docker-compose.yml` 暴露 `3443:443`
- 自签证书真机扫码验证通过

- **PDF 文本层 BOM 关键字段稳提 + `quantity` 文本纠偏**
- 先保证 `seq/code/product_code/name/quantity` 关键字段稳定拿到
- 文本层 `quantity` 允许覆盖 Vision 冲突值

---

## 📦 部署和升级提醒

1. **Docker 重新构建**
   - 这版包含前端 HTTPS、Nginx、证书文件、同源 API/WS/SSE 调整
   - 建议直接执行：

```bash
docker-compose down
docker-compose up -d --build
```

2. **访问地址变化**
   - HTTP：`http://localhost:3008`
   - HTTPS：`https://localhost:3443`

3. **自签证书注意事项**
   - 首次在手机端访问 HTTPS 时，需要先信任证书
   - 证书与固定内网 IP 强绑定，客户内网 IP 变更后需要重签
   - 第三方浏览器兼容性不稳定，建议优先用系统浏览器验收扫码

4. **无需数据库迁移**
   - 这版没有数据库结构迁移要求
   - 重点是前端访问协议、BOM 提取链路和产品步骤收口逻辑升级

---

## 🔗 相关链接

- **GitHub 仓库**: https://github.com/xiaotang-12-ops/yilite.git
- **Tag**: `v2.1.47`
- **Compare**: https://github.com/xiaotang-12-ops/yilite/compare/v2.1.42...v2.1.47
- **详细技术变更**: `Memory_Development/changelog.md`

---

## 📝 一句话总结

这版核心不是“多加几个功能点”，而是把 **扫码入口、BOM 文本提取、产品总装匹配、步骤标题口径** 这四条最容易影响交付质量的链路一起收口了，实际价值就是：**更容易部署、更容易扫码、更不容易串件、更不容易把错误标题直接交给工人看。**
